# Convolution

## Building

After installing [Rust](https://rustup.rs/), you can compile Convolution as follows:

```shell
cargo xtask bundle convolution_plug --release
```

SHIT

`Get-Content  "C:\Users\range\AppData\Local\Bitwig Studio\engine.log" -wait`
